<%= form_for [current_club, @team, @spot] do |f| %>
  <%= f.hidden_field :athlete_id, value: athlete.id %>
  <%= f.hidden_field :invite_status, value: :pending %>
  <%= f.hidden_field :team_id, value: @team.id %>
  <input type="submit" name="commit" value="Invite to Join" class="btn btn-success">
<% end %>
